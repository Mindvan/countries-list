import styled from 'styled-components'

const Wrap = styled.main`
  flex: 1;
  width: 100%;
  max-width: 1100px;
  margin: 0 auto;
  padding: 1rem 1.25rem 5rem;
`

export const Login = () => {
  return <Wrap>авторизация</Wrap>
}